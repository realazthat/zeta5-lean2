name: Soundness Guard

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  guard:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: leanprover/lean-action@v1

      - name: Install ripgrep
        run: sudo apt-get update && sudo apt-get install -y ripgrep

      - name: Guard - no user axioms in LeanCert
        run: |
          # Modifier-tolerant: also catches `private axiom`, `protected axiom`,
          # `@[attr] axiom`, etc. on one line. The whole-environment sweep in
          # Tests/AxiomAudit.lean is the semantic backstop for anything a
          # textual pattern can miss.
          if find LeanCert -name '*.lean' -print0 | xargs -0 grep -nE \
            '^[[:space:]]*((@\[[^]]*\]|private|protected|noncomputable|unsafe)[[:space:]]+)*axiom[[:space:]]+'; then
            echo "Found forbidden axiom declaration(s) in LeanCert/"
            exit 1
          fi

      - name: Guard - no non-allowlisted sorry in LeanCert
        run: |
          # Textual layer: catches standalone `sorry` lines and the common
          # inline forms (`:= sorry`, `by sorry`, `exact sorry`, `; sorry`).
          # The semantic layer is the sorryAx sweep in Tests/AxiomAudit.lean,
          # which pins an exact allowlist of declarations.
          # Allowlist (single file): LeanCert/CertifiedBounds/Li2.lean — the
          # PNT+ lightweight-interface split; its sorries are pinned by name
          # in Tests/AxiomAudit.lean and statement-identity-checked by the
          # Li2Verified target.
          hits="$(find LeanCert -name '*.lean' \
            ! -path 'LeanCert/CertifiedBounds/Li2.lean' -print0 | \
            xargs -0 grep -nE '^[[:space:]]*sorry\b|:=[[:space:]]*sorry\b|\b(by|exact)[[:space:]]+sorry\b|;[[:space:]]*sorry\b' || true)"
          if [ -n "$hits" ]; then
            echo "Found non-allowlisted sorry placeholder(s):"
            echo "$hits"
            exit 1
          fi

      - name: Guard - no synthetic proof holes in LeanCert
        run: |
          # Allowlist (single file): LeanCert/Tactic/Verification.lean — the
          # trust-classification module (`#assert_trust`) necessarily *names*
          # `sorryAx` in order to detect and reject it; it constructs nothing.
          # The sorryAx sweep in Tests/AxiomAudit.lean remains the semantic
          # backstop covering that file like any other.
          hits="$(find LeanCert -name '*.lean' \
            ! -path 'LeanCert/Tactic/Verification.lean' -print0 | \
            xargs -0 grep -nE '\bmkSorry\b|\bsorryAx\b|\badmit\b' || true)"
          if [ -n "$hits" ]; then
            echo "Found forbidden synthetic proof-hole construction(s):"
            echo "$hits"
            exit 1
          fi
          # Verification.lean itself: detection/classification only — still
          # forbid the constructors that *create* holes.
          hits="$(grep -nE '\bmkSorry\b|\badmit\b' LeanCert/Tactic/Verification.lean || true)"
          if [ -n "$hits" ]; then
            echo "Found forbidden synthetic proof-hole construction(s) in Verification.lean:"
            echo "$hits"
            exit 1
          fi

      - name: Audit - native/runtime boundary markers
        run: |
          echo "implemented_by uses:"
          find LeanCert -name '*.lean' -print0 | xargs -0 grep -nE '\bimplemented_by\b' || true
          echo "unsafe evalExpr uses:"
          find LeanCert/Tactic LeanCert/Meta -name '*.lean' -print0 | xargs -0 grep -nE '\bunsafe[[:space:]]+evalExpr\b|\bevalExpr\b' || true
          echo "pretty-printer string matching candidates:"
          find LeanCert/Tactic LeanCert/Meta -name '*.lean' -print0 | xargs -0 grep -nE 'ppExpr|endsWith "inst' || true

      - name: Build axiom audit imports
        run: |
          lake build LeanCert \
            LeanCert.Bridge LeanCert.Test.BridgeTest LeanCert.Test.Hardening \
            LeanCert.Engine.Eval LeanCert.Engine.Pisano LeanCert.Engine.PentagonLucas \
            LeanCert.Engine.TaylorModel.Log1p LeanCert.Engine.TaylorModel.TrigReduced \
            LeanCert.Core.HermiteBounds LeanCert.Core.LogBounds \
            LeanCert.CertifiedBounds.Li2 LeanCert.Examples.EulerMascheroniBounds

      - name: Guard - core axiom/sorry audit
        run: lake env lean Tests/AxiomAudit.lean

      - name: Guard - trust manifest for exported declarations
        run: lake env lean Tests/TrustManifest.lean
